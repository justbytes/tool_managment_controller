import { WorkOrder } from '../interface/interface'


const DbUpdater = async (data: WorkOrder): Promise<string> => {

    var x: string = "updating db"
    
    return x

} 

export default DbUpdater;